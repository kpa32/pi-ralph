# 开发实施计划

## 项目目标
为任务管理系统添加任务优先级功能，允许用户为任务设置高、中、低优先级，并基于优先级进行筛选和排序。

## 计划生成信息
- 生成时间: 2026-04-07 10:30:00
- 基于规格说明书: `.ralph/specs/01-task-priority.md`
- 当前代码状态: 现有任务系统包含任务标题、描述、状态（待办/进行中/完成），缺少优先级字段及相关UI。

## 待办事项列表（按优先级排序）

### 高优先级（依赖项，必须先完成）
1. [ ] **T-001: 添加优先级字段到数据库**
   - **描述**: 在 tasks 表中添加 priority 列，存储高、中、低三个值，默认值为“中”。
   - **验收标准**:
     - [ ] 创建数据库迁移文件，添加 priority 列（类型: VARCHAR(10) 或 ENUM）
     - [ ] 默认值设为 'medium'
     - [ ] 迁移可安全回滚
     - [ ] 类型检查通过
     - [ ] 现有测试通过（确保不破坏现有功能）
   - **依赖**: 无
   - **估计规模**: 小
   - **备注**: 使用 `IF NOT EXISTS` 确保迁移可重复执行。

2. [ ] **T-002: 更新任务模型和类型定义**
   - **描述**: 在应用代码中更新 Task 类型/模型，包含 priority 字段。
   - **验收标准**:
     - [ ] 更新 `src/lib/types/task.ts` 中的 Task 接口，添加 priority 属性
     - [ ] 更新 `src/lib/db/schema.ts` 中的任务模式（如果使用 ORM）
     - [ ] 更新相关的数据验证逻辑
     - [ ] 类型检查通过
   - **依赖**: T-001
   - **估计规模**: 小
   - **备注**: 确保类型定义与数据库列一致。

### 中优先级（核心功能）
3. [ ] **T-003: 在任务创建表单中添加优先级选择器**
   - **描述**: 在新建任务表单中添加优先级下拉菜单。
   - **验收标准**:
     - [ ] 表单中添加优先级字段，选项：高、中、低
     - [ ] 默认选中“中”
     - [ ] 表单提交时包含 priority 数据
     - [ ] 类型检查通过
     - [ ] 浏览器验证：使用 dev-browser 技能确认UI正常工作
   - **依赖**: T-002
   - **估计规模**: 小
   - **备注**: 复用现有的表单组件样式。

4. [ ] **T-004: 在任务编辑表单中添加优先级选择器**
   - **描述**: 在编辑任务模态框或页面中添加优先级字段。
   - **验收标准**:
     - [ ] 编辑表单中显示当前任务的优先级并允许修改
     - [ ] 保存后立即更新
     - [ ] 类型检查通过
     - [ ] 浏览器验证：使用 dev-browser 技能确认UI正常工作
   - **依赖**: T-003
   - **估计规模**: 小
   - **备注**: 确保编辑表单与创建表单保持一致。

5. [ ] **T-005: 在任务列表项中显示优先级徽章**
   - **描述**: 每个任务卡片/行中显示表示优先级的视觉徽章（例如颜色：红=高，黄=中，灰=低）。
   - **验收标准**:
     - [ ] 每个任务项旁显示彩色徽章
     - [ ] 徽章颜色与优先级对应（红/黄/灰）
     - [ ] 鼠标悬停显示优先级文本提示
     - [ ] 类型检查通过
     - [ ] 浏览器验证：使用 dev-browser 技能确认徽章显示正确
   - **依赖**: T-002
   - **估计规模**: 小
   - **备注**: 使用现有徽章组件，避免硬编码颜色。

### 低优先级（增强功能）
6. [ ] **T-006: 添加按优先级筛选任务的功能**
   - **描述**: 在任务列表上方添加筛选下拉菜单，允许按优先级筛选任务。
   - **验收标准**:
     - [ ] 筛选器包含选项：全部、高、中、低
     - [ ] 筛选状态保存在URL参数中
     - [ ] 筛选后列表正确更新
     - [ ] 类型检查通过
     - [ ] 浏览器验证：使用 dev-browser 技能确认筛选功能正常工作
   - **依赖**: T-005
   - **估计规模**: 中
   - **备注**: 可复用现有的筛选组件逻辑。

7. [ ] **T-007: 添加按优先级排序任务的功能**
   - **描述**: 在任务列表中添加排序选项，按优先级（高到低）排序。
   - **验收标准**:
     - [ ] 排序控件添加到列表头部
     - [ ] 排序顺序：高 > 中 > 低
     - [ ] 排序可与其他排序条件（如日期）结合
     - [ ] 类型检查通过
     - [ ] 浏览器验证：使用 dev-browser 技能确认排序功能正常工作
   - **依赖**: T-006
   - **估计规模**: 中
   - **备注**: 考虑使用现有的排序工具函数。

## 已完成事项
- 暂无

## 发现的问题与后续工作
- 当前任务模型未包含优先级字段，需要数据库迁移。（发现于: 2026-04-07）
  - 影响: 无法存储优先级数据
  - 建议解决方案: 按上述计划执行迁移。

## 计划更新记录
- 2026-04-07: 初始创建，基于规格说明书 `.ralph/specs/01-task-priority.md`

---

**计划使用说明**:
1. 任务按优先级排序，从上到下执行
2. 每个任务应小到能在一个迭代中完成
3. 任务完成后，将 `[ ]` 改为 `[x]` 并添加完成时间
4. 新发现的问题添加到"发现的问题与后续工作"部分
5. 计划可随时重新生成（计划可丢弃原则）

**注意事项**:
- 保持计划简洁，仅包含必要信息
- 依赖关系：数据库变更 → 后端逻辑 → 前端组件
- 每个任务必须有可验证的验收标准
- 遵循单一事实源原则，优先使用 `src/lib/` 共享组件