# 开发实施计划

## 项目目标
为任务管理系统添加任务优先级功能，允许用户为任务设置高、中、低优先级，并基于优先级进行筛选和排序。

## 计划生成信息
- 生成时间: 2026-04-07 11:45:00
- 基于规格说明书: `.ralph/specs/01-task-priority.md`
- 当前代码状态: 现有任务系统包含任务标题、描述、状态字段，缺少优先级字段及相关UI。

## 待办事项列表（按优先级排序）

### 高优先级（必须首先完成）
1. [ ] **T-001: 添加优先级字段到数据库**
   - **描述**: As a developer, I need to store task priority so it persists across sessions.
   - **验收标准**:
     - [ ] 在 tasks 表中添加 priority 列，类型为 `VARCHAR(10)` 或 `ENUM('high', 'medium', 'low')`
     - [ ] 默认值设为 'medium'
     - [ ] 创建并运行数据库迁移脚本
     - [ ] 迁移可安全回滚
     - [ ] 类型检查通过
   - **依赖**: 无
   - **优先级**: 1
   - **估计规模**: 小

2. [ ] **T-002: 更新任务模型和类型定义**
   - **描述**: As a developer, I need the application code to reflect the new priority field.
   - **验收标准**:
     - [ ] 更新 `src/lib/types/task.ts` 中的 Task 接口，添加 `priority: 'high' | 'medium' | 'low'` 属性
     - [ ] 更新 ORM 模型定义（如适用）
     - [ ] 更新任务创建/更新的验证逻辑
     - [ ] 类型检查通过
   - **依赖**: T-001
   - **优先级**: 2
   - **估计规模**: 小

### 中优先级（核心用户功能）
3. [ ] **T-003: 在任务创建表单中添加优先级选择器**
   - **描述**: As a user, I want to set priority when creating a new task.
   - **验收标准**:
     - [ ] 新建任务表单中包含优先级下拉菜单，选项：高、中、低
     - [ ] 默认选中“中”
     - [ ] 表单提交数据中包含 priority 字段
     - [ ] 类型检查通过
     - [ ] **浏览器验证**：使用 dev-browser 技能确认UI正常工作
   - **依赖**: T-002
   - **优先级**: 3
   - **估计规模**: 小

4. [ ] **T-004: 在任务编辑表单中添加优先级选择器**
   - **描述**: As a user, I want to change priority when editing an existing task.
   - **验收标准**:
     - [ ] 编辑表单中显示当前任务的优先级并允许修改
     - [ ] 保存后立即更新数据库
     - [ ] 类型检查通过
     - [ ] **浏览器验证**：使用 dev-browser 技能确认UI正常工作
   - **依赖**: T-003
   - **优先级**: 4
   - **估计规模**: 小

5. [ ] **T-005: 在任务列表项中显示优先级徽章**
   - **描述**: As a user, I want to see task priority at a glance in the task list.
   - **验收标准**:
     - [ ] 每个任务卡片/行旁显示彩色优先级徽章
     - [ ] 徽章颜色：红色=高，黄色=中，灰色=低
     - [ ] 鼠标悬停显示优先级文本提示
     - [ ] 类型检查通过
     - [ ] **浏览器验证**：使用 dev-browser 技能确认徽章显示正确
   - **依赖**: T-002
   - **优先级**: 5
   - **估计规模**: 小

### 低优先级（增强功能）
6. [ ] **T-006: 添加按优先级筛选任务的功能**
   - **描述**: As a user, I want to filter the task list to see only tasks of a certain priority.
   - **验收标准**:
     - [ ] 任务列表上方添加筛选下拉菜单，选项：全部、高、中、低
     - [ ] 筛选状态保存在 URL 参数中
     - [ ] 筛选后列表正确更新
     - [ ] 类型检查通过
     - [ ] **浏览器验证**：使用 dev-browser 技能确认筛选功能正常工作
   - **依赖**: T-005
   - **优先级**: 6
   - **估计规模**: 中

7. [ ] **T-007: 添加按优先级排序任务的功能**
   - **描述**: As a user, I want to sort tasks by priority (high to low) within each status column.
   - **验收标准**:
     - [ ] 任务列表中添加排序控件
     - [ ] 排序顺序：高 > 中 > 低
     - [ ] 排序可与其他排序条件结合
     - [ ] 类型检查通过
     - [ ] **浏览器验证**：使用 dev-browser 技能确认排序功能正常工作
   - **依赖**: T-006
   - **优先级**: 7
   - **估计规模**: 中

## 已完成事项
- 暂无

## 发现的问题与后续工作
- 当前任务模型未包含优先级字段，需要数据库迁移。（发现于: 2026-04-07）
  - 影响: 无法存储优先级数据
  - 建议解决方案: 按上述计划执行迁移。

## 计划更新记录
- 2026-04-07: 初始创建，基于规格说明书 `.ralph/specs/01-task-priority.md`

---

**计划使用说明**:
1. 任务按优先级（数字）排序，从上到下执行；每次迭代只处理一个任务。
2. 每个任务必须小到能在一个上下文窗口内完成（通常为 1-2 个文件变更）。
3. 任务完成后，将 `[ ]` 改为 `[x]`，并可在下方添加完成时间注释。
4. 新发现的问题添加到“发现的问题与后续工作”部分。
5. 如果计划过时或错误，可随时丢弃并重新生成（计划可丢弃原则）。

**注意事项**:
- 保持计划简洁，仅包含必要信息。
- 依赖关系：数据库变更 → 后端逻辑 → 前端组件 → 聚合视图。
- 每个任务必须有可验证的验收标准，UI 任务必须包含浏览器验证。
- 遵循单一事实源原则，优先使用 `src/lib/` 共享组件。
- 验收标准中的“类型检查通过”是必须项。

**学习记录**:
- 每次迭代后，在 `ralph_agent.md` 中追加学习内容（模式、坑点、上下文）。
- 通用模式添加到 `ralph_agent.md` 顶部的“Codebase Patterns”部分。